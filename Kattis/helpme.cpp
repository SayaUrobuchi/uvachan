#include <stdio.h>

int main()
{
	while (scanf("%s", buf) == 1)
	{
		wn = 0;
		bn = 0;
		for (i=0; i<8; i++)
		{
			scanf("%s%*s", buf);
			for (j=2; j<=2+4*7; j+=4)
			{
				
			}
		}
	}
	return 0;
}
