#include <iostream>
using namespace std;

const int N = 128;

char s[N];

int main()
{
	int n, m, i;
	scanf("%d%d", &n, &m);
	for (i=0; i<n; i++)
	{
		scanf("%s", s);
		puts(s);
		puts(s);
	}
	return 0;
}
